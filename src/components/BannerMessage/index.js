import React from 'react'

import './style.css'

const BannerMessage = ({ message }) => (
  <div className='BannerMessage'>
    You just created a timeline, keep the address safe
  </div>
)

export default BannerMessage
